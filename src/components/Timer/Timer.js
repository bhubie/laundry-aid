import React, { Component } from 'react';
import './Timer.css';

export const Timer = (props) => {
    return (
        <div className="Timer">
           <h1>{this.props.timeRemaining}</h1>
        </div>
        );
};
